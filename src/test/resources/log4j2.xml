<?xml version="1.0" encoding="UTF-8"?>

<Configuration status="Warn"> <!-- Here u can use multiple log levels better to use warn/error to reduce red log msgs in console -->

<Properties>
	<Property name="log-path">./logs</Property> <!-- Location of file-->
	<Property name="log-pattern">%d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n</Property>
</Properties>


<!-- Appenders - where to generate logs (Console/File) -->
 <Appenders>
	<Console name="Console" target="SYSTEM_OUT">
		<PatternLayout pattern="${log-pattern}"/>
	</Console>
	
	<RollingFile name="File" fileName="${log-path}/automation.log" filePattern="${log-path}/automation-%d{MM-dd-yyyy}-%i.log.gz">
		<PatternLayout pattern="${log-pattern}"/>
		<Policies>
			<TimeBasedTriggeringPolicy/>
			<SizeBasedTriggeringPolicy size="1MB"/>
		</Policies>
		<DefaultRolloverStrategy max="10"/>
	</RollingFile>
	
 </Appenders>


<!-- Loggers  - what type of logs generate (All < Trace < Debug < Info < Warn < Error < Fatal < Off) -->
 <Loggers>
	<Root level="Info"> <!-- Here u can use multiple log levels -->
	<!-- <AppenderRef ref="Console"/> -->
	<AppenderRef ref="File"/>
	</Root>
 </Loggers>
</Configuration>